# AE Sentinel 版本更新日志

> 药物临床试验不良事件智能监测平台 — 版本变更记录

---

## v1.2.2 (2026-06-10) — 服务器内部错误紧急修复

### 根因

`database.py` 中 `execute_query()` / `execute_insert()` / `init_db()` 的 SQLite 路径错误地将 `sqlite3.Connection` 对象（而非 Cursor）赋值给 `cursor` 变量，导致 `.fetchall()` / `.lastrowid` 调用失败。该问题影响所有依赖数据库查询的 API 端点，表现为前端统一返回 `500 Internal Server Error`。

### 后端修复 (5 文件)

| 文件 | 问题 | 修复 |
|---|---|---|
| `backend/core/database.py` | ① SQLite 路径 `cursor = conn` 导致 `fetchall()` 失败 ② 旧数据库缺少 `patient_gender`/`patient_dob`/`deadline` 列 | ① 统一改为 `cursor = conn.cursor()` ② `init_db()` 新增 ALTER TABLE 迁移块，幂等添加缺失列 |
| `backend/services/audit_service.py` | `write_audit_log()` INSERT 使用 ISO 格式 `YYYY-MM-DDTHH:MM:SSZ`，MySQL DATETIME 列拒绝 | 改为 MySQL 兼容格式 `YYYY-MM-DD HH:MM:SS` |
| `backend/services/knowledge_service.py` | `process_uploaded_file()` INSERT 同上日期格式问题 | 同上 |
| `backend/api/ae.py` | `process_ae()` 直接访问 `req.patient_id`/`req.visit_date`/`req.drug_name` 等属性，但端点未对缺失字段设默认值，导致 `AttributeError` | `/process` 和 `/batch` 端点补齐 9 个字段默认值 |
| `backend/agents/compliance.py` | `get_compliance_report()` 未返回分项得分，前端无法独立展示及时性/完整性评分 | 响应新增 `timeliness_score` 和 `completeness_score` |

### 前端修复 (3 文件)

| 文件 | 问题 | 修复 |
|---|---|---|
| `frontend/src/views/SaeReports.vue` | ① 状态列引用 `row.status`（后端返回 `report_status`）② 详情弹窗引用不存在的 `reporter_name`/`reporter_org` | ① 改为 `row.report_status` ② 替换为 `patient_gender`/`patient_dob`（CIOMS-I 新增字段） |
| `frontend/src/views/Audit.vue` | 列绑定字段名与后端 API 不一致 | `username`→`user_id`, `agent`→`agent_type`, `resource`→`resource_id`, `timestamp`→`created_at` |
| `frontend/src/views/Compliance.vue` | ① 进度环绑定 `data.sae_timeliness`（数组）做数值运算 ② `remaining_days`→`days_remaining` ③ issues 作为字符串数组但模板期望对象 | ① 综合/及时性/完整性三个进度环分别绑定正确数值字段 ② 列名修正 ③ 将 issues 字符串映射为 `{description, severity, source}` 对象 |

### 验证结果

全部 12 个 API 端点通过端到端测试（含中文 AE 文本 MedDRA 编码）：

| 端点 | 状态 |
|---|---|
| `POST /api/auth/login` | ✅ SHA256 密码认证 |
| `POST /api/ae/process` (中文) | ✅ 头痛/发热/咳嗽 → 神经系统/全身/呼吸 SOC |
| `GET /api/ae/results` | ✅ 13 条记录 |
| `POST /api/saereport/generate` | ✅ SAE 报告生成 + CIOMS-I 字段 |
| `GET /api/saereport/list` | ✅ 4 条报告 |
| `GET /api/compliance/report` | ✅ 综合评分 0.25 |
| `GET /api/deviations/list` | ✅ 2 条偏离 + 7 条规则 |
| `GET /api/signals/list` | ✅ 8 条信号 |
| `POST /api/signals/trigger` | ✅ 后台线程触发 |
| `GET /api/admin/knowledge/list` | ✅ 知识库列表 |
| `GET /api/admin/knowledge/search` | ✅ 向量检索 |
| `GET /api/admin/audit-logs` | ✅ 10 条审计日志 |

---

## v1.2.1 (2026-06-10) — P1/P2 全面修复

### 医药领域补充真实数据

- **MedDRA 词库**：30→104 条，覆盖 18 个 SOC（感染/血液/免疫/内分泌/代谢/精神/神经/眼/心脏/血管/呼吸/胃肠/肝胆/皮肤/骨骼/肾/全身/检查）
- **子串误匹配修复**：长词优先匹配 + 区间去重（如"免疫相关肝炎"优先于"肝炎"）
- **信号挖掘**：移除捏造 p 值，实现 PRR（EMA 标准）+ ROR（95%CI）+ Chi2 真实统计算法
- **暴露人数**：改用 DB DISTINCT 统计，10 个器官系统信号定义
- **因果关系**：升级为 WHO-UMC 6 级评估（certain/probable/possible/unlikely/conditional/unassessable）
- **CIOMS-I 报告**：新增 patient_gender/patient_dob 字段，补全 study_number（TRIAL_NAME），新增 24h 快速报告时限

### 安全加固

- **密码哈希**：MOCK_USERS 改用 SHA256 哈希存储，默认密码 pv123456/cra123456/admin123456
- **速率限制**：新增 `rate_limit.py` 中间件（120 req/min），注册到 FastAPI
- **文件上传**：类型白名单（.md/.csv/.txt/.pdf/.json）+ 10MB 大小限制
- **角色权限**：新增 `auth_middleware.py`，pv_specialist/cra/admin 三级权限映射

### 产品功能

- **知识库搜索**：新增 `GET /api/admin/knowledge/search`（向量检索）
- **AE 表单**：前端 + 后端新增性别/出生日期字段，流程贯通至 CIOMS-I 报告
- **数据库**：ae_results 表新增 patient_gender/patient_dob 列

### 改进项

- e2e 测试密码同步更新

### 变更文件

`backend/agents/ae_coder.py`, `backend/agents/sae_report.py`, `backend/agents/signal_agent.py`, `backend/api/auth.py`, `backend/api/knowledge.py`, `backend/core/auth_middleware.py`, `backend/core/config.py`, `backend/core/database.py`, `backend/core/rate_limit.py`, `backend/main.py`, `frontend/src/views/AeCoding.vue`, `test_e2e.py`

---

## v1.2.0 (2026-06-10) — 交付验收 P0/P1 修复

### P0 修复（阻塞交付）

| 问题 | 修复 |
|---|---|
| SECRET_KEY / API_KEYS 硬编码在 config.py | 改为环境变量读取，新增 `.env.example` 模板 |
| health.py SQLite 下 `conn.cursor()` 崩溃 | 修复分引擎连接检查逻辑 |
| LLM/ChromaDB 健康检查返回虚假"healthy" | 改为真实连接检查（`list_collections()` / `SELECT 1`） |
| AeCoding.vue MedDRA 编码显示 `[object Object]` | 模板中正确展开 `pt_name` / `llt_name` |
| deviation.py PD-003 随机 15% 模拟逻辑 | 移除随机逻辑，标注需接入实际数据 |
| signal_agent.py 捏造 p 值公式和随机 PubMed 文献 | 改为明确的模拟标注 |

### P1 修复

| 问题 | 修复 |
|---|---|
| CORS 硬编码 `allow_origins=["*"]` | 改为通过 `CORS_ORIGINS` 环境变量配置 |
| SAE 标准缺少「先天性畸形」 | 补全 ICH E2A 第 6 条标准 |
| CIOMS-I 字段缺少 study_number | 补全 TRIAL_NAME + 24h 快速报告时限 |
| 缺少 SAE 提交状态流转端点 | 新增 `POST /api/saereport/{id}/submit`（draft→submitted） |
| auth.py 冗余 `jwt as pyjwt` 双重 import | 清理 |
| deviation.py / signal_agent.py 未使用的 import | 清理 `random` / `timedelta` |

### 文档

- 新增 `DELIVERY_REVIEW.md`：四维专家验收报告（医药/技术/产品/法务），含 7 个 P0 + 10 个 P1 + 10 个 P2 清单

### 变更文件

`backend/core/config.py`, `backend/api/health.py`, `frontend/src/views/AeCoding.vue`, `backend/agents/deviation.py`, `backend/agents/signal_agent.py`, `backend/main.py`, `backend/agents/ae_coder.py`, `backend/agents/sae_report.py`, `backend/api/sae.py`, `backend/api/auth.py`, `DELIVERY_REVIEW.md`

---

## v1.1.0 (2026-06-07 ~ 2026-06-09) — 基础设施完善

### 数据库

- 增强 SQLite 兼容层转换规则（`_to_sqlite_sql()`）：`%s`→`?`，`TINYINT`→`INTEGER`，`DOUBLE`→`REAL`，`VARCHAR(N)`→`TEXT`，`DATETIME DEFAULT NOW()`→`TEXT DEFAULT (datetime('now','localtime'))`
- 通过 `DATABASE_ENGINE` 环境变量切换 MySQL / SQLite

### 导入路径统一

- 全项目统一为 `backend.xxx` 绝对导入路径
- 补全 `core/` / `api/` / `agents/` / `services/` 的 `__init__.py`

### 审计日志

- 实现审计日志自动记录功能（`audit_service.py`）
- ID 格式 `AUD-{12位HEX}`
- JWT 提取用户 → `write_audit_log()` 写入 `audit_logs` 表

### 前端架构

- Vue3 Composition API + Pinia + Element Plus + ECharts + Vite
- 8 个页面组件（懒加载）+ AppLayout 主布局
- axios 拦截器自动附加 Bearer token + 401 跳转登录

---

## v1.0.0 — 项目初始化

### 核心功能

- **AE 编码**：MedDRA 关键词匹配，严重性/SAE/预期性/因果关系判定
- **SAE 报告**：CIOMS-I 格式自动生成，支持 docx/json/pdf 导出
- **方案偏离检测**：7 条默认规则（PD-001 ~ PD-007），AE 编码后自动联动检测
- **安全性信号挖掘**：后台线程 PRR/ROR/Chi2 统计
- **合规质控**：时限监控 + 字段完整性检查
- **知识库管理**：文档上传 + ChromaDB 向量化 + 向量检索
- **审计日志**：全操作自动记录
- **Mock JWT 认证**：HS256，三级角色

### 技术栈

- **后端**：Python 3.9+ / FastAPI / Uvicorn / ChromaDB / PyMySQL (vendored) / PyJWT (vendored)
- **前端**：Vue3 / Pinia / Element Plus / ECharts / Vite / axios
- **数据库**：MySQL (生产) / SQLite (本地开发)
- **容器化**：Docker Compose (应用 + ChromaDB + 可选 Ollama)

### 架构

```
请求 → FastAPI Router (api/) → Agent (agents/) → Database (database.py)
                                ↕
                       Service (services/)
```

---

## 已知问题 & 下一步计划

### P2 待执行

- [ ] 真实 LLM 接入（ZHIPU/QWEN API key 已预留）
- [ ] PDF 真实渲染（当前为 docx 占位）
- [ ] 真实 FAERS 数据导入（文件已在 `data/downloads/`）
- [ ] PubMed E-utilities API 真实检索
- [ ] 前端 E2E 测试覆盖
- [ ] CI/CD 流水线

### v1.3 规划

- [ ] 患者人口学信息独立表
- [ ] AE/SAE 报告审批工作流
- [ ] 数据看板时间范围筛选
- [ ] 离线模式（纯 SQLite + 内嵌向量检索）

---

> **维护者**：it-xian
> **最后更新**：2026-06-10
